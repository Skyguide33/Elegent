//2025/4/20
#include <bits/stdc++.h>
using namespace std;

int a[1005][1005];

int main() {
  cin.tie(0) -> sync_with_stdio(0);
  int T; cin >> T;
  while(T--) {
    int n, m; cin >> n >> m;
    memset(a, 0, sizeof(a));
    int tag_num = n*n - m;
  }
  return 0;
}