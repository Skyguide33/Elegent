//2025/5/1
#include <bits/stdc++.h>
using namespace std;

#define int long long

int x[500005], s[500005];

signed main() {
  cin.tie(0) -> sync_with_stdio(0);
  int n, d, k; cin >> n >> d >> k;
  int ans = 0;
  for(int i = 1; i <= n; ++i) {
    cin >> x[i] >> s[i];
  }
  return 0;
}